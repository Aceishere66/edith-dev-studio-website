{
  "name": "edith-dev-studio-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.5.1",
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.3.3"
  },
  "devDependencies": {
    "wrangler": "^4.91.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
